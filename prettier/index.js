// @ts-check

/** @type {import("prettier").Config} */
const config = {
  "arrowParens": "always",
  "printWidth": 100,
  "singleQuote": true,
  "quoteProps": "consistent"
};

module.exports = config;
